import React from 'react';
import { IonContent, IonPage, IonText } from '@ionic/react';
import './Cuenta.scss';

const Cuenta: React.FC = () => {

    return (
        <IonPage>
            <IonContent fullscreen>
                <IonText>Cuenta Works!!!</IonText>
            </IonContent>
        </IonPage>
    );
}

export default Cuenta;